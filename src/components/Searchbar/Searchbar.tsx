import { Button } from "@mui/material";

function Searchbar() {
  return (
    <>
      <Button variant="contained" size="large" sx={{backgroundColor: "orange"}}>Filtruj</Button>
    </>
  );
}

export default Searchbar;
